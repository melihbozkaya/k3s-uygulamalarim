apiVersion: v1
kind: Service
metadata:
  name: jellyfin-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: jellyfin
  ports:
  - port: 8096
    targetPort: 8096
    nodePort: 30096
